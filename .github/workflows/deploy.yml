name: Deploy Podman Compose

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, s601-edu-01]

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Deploy via Podman Compose
        run: |
          cd ./API-Compressor
          podman-compose down -v
          podman-compose up -d --build
